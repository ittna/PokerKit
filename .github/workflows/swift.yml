name: Swift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
      with:
        lfs: true
    - run: git lfs checkout
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
